query QueryAllBusinesses {
  businesses {
    ... BusinessInfo
  }
}

query QueryBusinesses($near: NearLocationInput, $districts: [String!], $sort: BusinessSortBy) {
  businesses(near: $near, districts: $districts, sort: $sort) {
    ... BusinessInfo
  }
}
