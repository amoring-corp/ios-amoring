mutation CreateCheckInByToken($token: String!) {
  createCheckInByToken(token: $token) {
    id
    businessId
    business {
      id
      ownerId
      businessName
    }
    profileId
    profile {
      id
    }
    status
    checkedInAt
    checkedOutAt
    createdAt
    updatedAt
  }
}
